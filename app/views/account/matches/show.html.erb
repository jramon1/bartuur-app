<%= render 'shared/navbars/arrow_user_product' %>

<div class="container-chat">

  <p>Match since <%= @match_since %> </p>
  <hr>


  <% @match.messages.each do |message| %>
  <% if message.user_id == @other_user.id %>
  <p class="them">
    <%= message.content %>
  </p>
  <% else %>
  <p class="me">
    <%= message.content %>
  </p>
  <% end %>
  <% end %>

  <%= simple_form_for [@match, @message], url: account_match_messages_path(@match) do |f| %>
  <%=f.input :content, as: :text, input_html: {rows: 2, cols: 10} %>
  <div class="form-actions">
    <%= f.submit %>
  </div>

  <% end %>
</div>
